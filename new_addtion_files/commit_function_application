这个问题的根本原因是**并发请求导致 sha 不一致**——Streamlit 有时候会重复执行代码，导致两个请求同时去拿 sha，拿到的结果不一样。

**解决方案：上传失败时自动重试一次，用户完全无感知：**

把上传那段改成：

```python
def _upload_to_github(api_url, headers, payload, data):
    """上传，失败时自动重试一次获取最新sha"""
    response = requests.put(api_url, headers=headers, json=payload)
    
    if response.status_code in [200, 201]:
        return True
    
    # 失败了就重新获取最新sha再试一次
    check = requests.get(api_url, headers=headers, params={"ref": GITHUB_BRANCH})
    sha = check.json().get("sha") if check.status_code == 200 else None
    if sha:
        payload["sha"] = sha
    
    retry = requests.put(api_url, headers=headers, json=payload)
    return retry.status_code in [200, 201]
```

然后原来调用的地方改成：

```python
success = _upload_to_github(api_url, headers, payload, data)

if success:
    st.success("活动信息上传成功！")
    if "activities" in st.session_state:
        del st.session_state["activities"]
else:
    st.error("上传失败，请重试")
```

这样第一次失败会自动静默重试，用户只会看到最终结果，不会看到报错了。
